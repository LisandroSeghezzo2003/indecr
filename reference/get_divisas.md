# Listar divisas disponibles en la API del BCRA

Listar divisas disponibles en la API del BCRA

## Usage

``` r
get_divisas()
```

## Value

Un tibble con columnas `codigo` y `descripcion`.

## Examples

``` r
if (FALSE) { # \dontrun{
get_divisas()
} # }
```
