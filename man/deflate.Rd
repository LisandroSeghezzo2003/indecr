% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deflate.R
\name{deflate}
\alias{deflate}
\title{Deflactar una serie nominal a valores reales}
\usage{
deflate(x, fechas, base = NULL, serie_ipc = "nacional")
}
\arguments{
\item{x}{Numeric. Vector de valores nominales a deflactar.}

\item{fechas}{Date o Character. Vector de fechas correspondientes a
cada valor de \code{x}. Deben estar en formato \code{"YYYY-MM-DD"} o ser
objetos Date. Deben tener la misma longitud que \code{x}.}

\item{base}{Date o Character. Fecha base en la que se expresan los
valores reales. Por defecto \code{NULL}, que devuelve valores en terminos
del indice original (base dic 2016 = 100). Ejemplo: \code{"2022-01-01"}
expresa todos los valores en pesos de enero 2022.}

\item{serie_ipc}{Character. Serie del IPC a usar para deflactar.
Por defecto \code{"nacional"}. Ver \code{\link{get_ipc}} para opciones.}
}
\value{
Un vector numerico de la misma longitud que \code{x} con los valores
deflactados.
}
\description{
Convierte una serie de valores nominales a valores reales usando el IPC
nacional base diciembre 2016. Permite expresar los valores en cualquier
fecha base.
}
\examples{
\dontrun{
# Deflactar salarios a pesos de enero 2022
salarios <- get_salarios(desde = "2020-01-01")
salarios$real <- deflate(
  x      = salarios$valor,
  fechas = salarios$fecha,
  base   = "2022-01-01"
)

# Deflactar canasta basica
canasta <- get_canasta(desde = "2020-01-01")
canasta$real <- deflate(canasta$valor, canasta$fecha, base = "2020-01-01")
}
}
